
<h3><%= @user.name+"\'s" + " " 'details' %> </h3>
  <% @albums.each do|album|%>
  <h4>Album name: <%=link_to album.name, album_path(album) %></h4>
<%end%>

<h4>Camera used:</h4>

  <% @user.cameras.each do |camera| %>
  <%= camera.model%>
  <%= camera.description%>
<%end%>
